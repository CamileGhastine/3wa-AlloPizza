<table class="table table-hover">
    <thead>
    <tr>
        <th scope="col">product</th>
        <th scope="col">quantité</th>
        <th scope="col">prix</th>
    </tr>
    </thead>
    <tbody>
    {% for product in cart.order %}
        <tr class="table-active">
            <td>{{ product.product.name }}</td>
            <td>
                <a href="{{ path('sub_cart', {'id' : product.product.id}) }}"><span class="badge bg-success">-</span></a>
                <span class="badge bg-secondary">{{ product.quantity }}</span>
                <a href="{{ path('add_cart', {'id' : product.product.id}) }}"><span class="badge bg-success">+</span></a>
                <a href="{{ path('remove_item', {'id' : product.product.id}) }}"><span class="badge bg-danger">🗑️</span></a>
            </td>
            <td>
                {{ product.product.price * product.quantity /100 }} €
            </td>

        </tr>
    {% endfor %}
    <tr>
        <td colspan="2">Total</td>
        <td>{{ cart.total /100 }} €</td>
    </tr>
    </tbody>
</table>